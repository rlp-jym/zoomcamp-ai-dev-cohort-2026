import '../styles/IdleScreen.css'

export function IdleScreen(): JSX.Element {
  return (
    <section className="idle-screen" aria-label="Idle">
      <p className="idle-text">No live match</p>
    </section>
  )
}
